from setuptools import setup

# Dynamic configuration is handled by pyproject.toml in Python 3.13.
# This minimal stub ensures backward compatibility with legacy installation scripts.
if __name__ == "__main__":
    setup()
